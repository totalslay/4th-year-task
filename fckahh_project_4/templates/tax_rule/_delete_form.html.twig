<form method="post" action="{{ path('app_tax_rule_delete', {'id': tax_rule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tax_rule.id) }}">
    <button class="btn">Delete</button>
</form>
