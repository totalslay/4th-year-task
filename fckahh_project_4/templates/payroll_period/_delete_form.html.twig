<form method="post" action="{{ path('app_payroll_period_delete', {'id': payroll_period.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payroll_period.id) }}">
    <button class="btn">Delete</button>
</form>
